// This script was a temporary TypeScript helper for prompt testing.
// The repository now keeps a CommonJS version at `scripts/run_prompt.cjs` for quick checks.
// Remove this file if you no longer need it.

export {}
